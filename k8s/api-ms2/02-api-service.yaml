apiVersion: v1
kind: Service
metadata:
  name: api-ms2-service
  namespace: api-test-namespace-dev
  annotations:
    ingress.kubernetes.io/service-upstream: "true"
spec:
  type: ClusterIP
  selector:
    app: api-ms2
  ports:
    - name: http-web
      port: 80
      targetPort: 3000
      protocol: TCP
